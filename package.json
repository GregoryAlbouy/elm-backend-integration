{
  "scripts": {
    "build": "ts-node --project ./script/tsconfig.json ./script/build.ts",
    "start": "NODE_ENV=production npm run build && node dist/app.js",
    "dev": "NODE_ENV=development npm run build"
  },
  "dependencies": {
    "fastify": "^4.0.0"
  },
  "devDependencies": {
    "@types/elm": "^0.19.1",
    "@types/node": "^20.1.4",
    "elm": "^0.19.1-5",
    "elm-test": "^0.19.1-revision12",
    "esbuild": "^0.17.19",
    "esbuild-plugin-elm": "^0.0.12",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
